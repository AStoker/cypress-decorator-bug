
import { TestAttribute } from './test-attribute';

describe('Test Attribute', () => {
    it('exists', () => {
        let testAttribute = new TestAttribute();
        expect(testAttribute).toBeDefined();
    });
});
